[build]
builder = "RAILPACK"
buildCommand = "npm run build:ws"

[deploy]
startCommand = "node dist/out-tsc/apps/websocket-gateway/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
