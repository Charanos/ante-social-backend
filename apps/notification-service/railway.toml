[build]
builder = "NIXPACKS"
buildCommand = "npm run build:notification"

[deploy]
startCommand = "node dist/out-tsc/apps/notification-service/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
