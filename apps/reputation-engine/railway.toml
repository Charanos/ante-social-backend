[build]
builder = "NIXPACKS"
buildCommand = "npm run build:reputation"

[deploy]
startCommand = "node dist/out-tsc/apps/reputation-engine/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
