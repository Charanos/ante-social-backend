[build]
builder = "RAILPACK"
buildCommand = "npm run build:gateway"

[deploy]
startCommand = "node dist/out-tsc/apps/api-gateway/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
