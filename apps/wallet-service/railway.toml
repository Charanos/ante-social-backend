[build]
builder = "NIXPACKS"
buildCommand = "npm run build:wallet"

[deploy]
startCommand = "node dist/out-tsc/apps/wallet-service/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
