[build]
builder = "RAILPACK"
buildCommand = "npm run build:market"

[deploy]
startCommand = "node dist/out-tsc/apps/market-engine/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
