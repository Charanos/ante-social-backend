[build]
builder = "NIXPACKS"
buildCommand = "npm run build:auth"

[deploy]
startCommand = "node dist/out-tsc/apps/auth-service/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
