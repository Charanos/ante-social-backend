[build]
builder = "NIXPACKS"
buildCommand = "npm run build:admin"

[deploy]
startCommand = "node dist/out-tsc/apps/admin-service/src/main.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
